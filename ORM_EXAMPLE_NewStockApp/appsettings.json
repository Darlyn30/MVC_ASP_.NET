{
    "Logging": {
        "LogLevel": {
            "Default": "Information",
            "Microsoft.AspNetCore": "Warning"
        }
    },
    "AllowedHosts": "*",
    //connection server for Db, then we use env vars in User Secrets
    "ConnectionStrings": {
        // i deleted Data for security
        "AppConnection": ""
    }

}
